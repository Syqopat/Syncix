# Syncix project settings
sync_dir = "example"
port = 8080

# Paths left out of sync (glob).
# These paths are neither read nor deleted. Note that Syncix only touches files
# in formats it could have produced itself (.json .lua .luau .txt .meta.json),
# so files such as a README are safe even without this list.
ignore = ["notes/**"]
